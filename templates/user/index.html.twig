{% extends 'base.html.twig' %}
{% block body %}
    <div class="display-3 text-center p-5">Liste des utilisateurs</div>
    <table class="table table-striped table-hover">
        <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">prénom</th>
            <th scope="col">nom</th>
            <th scope="col">email</th>
            <th scope="col">acions</th>
        </tr>
        </thead>
        <tbody>
        {% for user in userArray %}
            <tr>
                <th>{{ user.id }}</th>
                <td>{{ user.firstName }}</td>
                <td>{{ user.lastName }}</td>
                <td>{{ user.email }}</td>
                <td>
                    <a href="{{ path('editUser',{'id': user.id}) }}"><i class="fas fa-edit btn btn-warning mr-2"></i></a>
                    <a href="#"><i class="fas fa-trash-alt btn btn-danger"></i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
